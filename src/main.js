import { createApp } from 'vue'
import App from './App.vue'
// import { data } from './data'
// import getMessage from './function';

createApp(App).mount('#app')

// getMessage(data.message1);
// getMessage(data.message2);